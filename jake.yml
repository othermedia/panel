--
source_directory:   source
build_directory:    build
layout:             together
builds:
  src:
    shrink_vars:    false
    private:        false
  min:
    shrink_vars:    true
    private:        true
packages:
  panel:
    files:
      - panel
    meta:
      provides:
        - panel
        - Panel
        - PanelOverlay
      requires:
        - JS.Class
        - Ojay
        - Ojay.HTML
        - Ojay.ContentOverlay
